# 📋 **阶段二独立开发测试数据插入策略**

## 🗃️ **数据库状态**
- **A使用**：ry-vue（已有用户数据）+ 新建业务表
- **B使用**：ry-vue（已有用户数据）+ 新建业务表  
- **各自独立**，互不影响

---

## 📝 **A的数据插入策略**

### **插入时机：第2天上午创建完表后立即执行**
```bash
# 1. 创建A的测试数据脚本
cat > A_insert_test_data.sql << 'EOF'
-- A的独立测试数据（ry-vue数据库）
USE ry-vue;

-- A负责的表：term, course, course_class, credit_rule
-- 清空自己的表（不影响B的表）
DELETE FROM term;
DELETE FROM course;
DELETE FROM course_class;
DELETE FROM credit_rule;

-- 插入学期数据
INSERT INTO term (term_id, term_name, enroll_start, enroll_end, drop_deadline, is_active) VALUES 
('2024-2025-1', '2024-2025秋季', '2024-09-01', '2024-09-30', '2024-10-15', 1),
('2024-2025-2', '2024-2025冬季', '2025-02-01', '2025-02-28', '2025-03-15', 0);

-- 插入课程数据  
INSERT INTO course (course_code, course_name, credit_hours, course_type) VALUES
('CS101', '计算机基础', 3, '必修'),
('MATH201', '高等数学', 4, '必修'),
('ENG301', '大学英语', 2, '必修');

-- 插入教学班（假设sys_user中有ID为1001-1003的教师）
INSERT INTO course_class (course_id, teacher_id, term_id, class_time, location, capacity) VALUES
(1, 1001, '2024-2025-1', '周一1-2节', '教学楼A201', 60),
(2, 1002, '2024-2025-1', '周二3-4节', '教学楼B301', 50),
(3, 1003, '2024-2025-1', '周三5-6节', '教学楼C101', 40);

-- 插入学分规则
INSERT INTO credit_rule (rule_name, term_id, max_credits) VALUES
('2024秋季规则', '2024-2025-1', 20);
EOF

# 2. 执行插入命令
mysql -u root -p你的密码 < A_insert_test_data.sql

# 3. 验证插入成功
mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'term表' as table, COUNT(*) as count FROM term UNION SELECT 'course表', COUNT(*) FROM course UNION SELECT 'course_class表', COUNT(*) FROM course_class;"
```

### **A的每日备份脚本**
```bash
# 每天开始工作前备份
#!/bin/bash
BACKUP_DIR="/path/to/backup"
DATE=$(date +%Y%m%d_%H%M%S)

# 备份A负责的表
mysqldump -u root -p你的密码 ry-vue term course course_class credit_rule > ${BACKUP_DIR}/ry-vue_A_${DATE}.sql
echo "A的备份完成: ry-vue_A_${DATE}.sql"

# 执行命令
chmod +x A_backup.sh
./A_backup.sh
```

### **A验证数据命令**
```bash
# 验证A的所有数据
mysql -u root -p你的密码 << 'EOF'
USE ry-vue;

-- 快速验证
SELECT '=== A的数据验证 ===' as title;
SELECT 'term表' as table_name, COUNT(*) as row_count FROM term
UNION ALL
SELECT 'course表', COUNT(*) FROM course  
UNION ALL
SELECT 'course_class表', COUNT(*) FROM course_class
UNION ALL
SELECT 'credit_rule表', COUNT(*) FROM credit_rule;

-- 查看样本数据
SELECT '样本数据-term:' as sample;
SELECT term_id, term_name, is_active FROM term LIMIT 2;
EOF
```

---

## 📝 **B的数据插入策略**

### **插入时机：第2天下午创建完enrollment表后立即执行**
```bash
# 1. 创建B的测试数据脚本
cat > B_insert_test_data.sql << 'EOF'
-- B的独立测试数据（ry-vue数据库）
USE ry-vue;

-- B负责的表：enrollment, class_time_slot
-- 清空自己的表
DELETE FROM enrollment;
DELETE FROM class_time_slot;

-- 先为B创建独立的测试课程（不依赖A）
-- 插入B自己的学期数据
INSERT INTO term (term_id, term_name, is_active) 
SELECT 'B-2024-2025-1', 'B测试学期', 1
WHERE NOT EXISTS (SELECT 1 FROM term WHERE term_id = 'B-2024-2025-1');

-- 插入B自己的课程
INSERT INTO course (course_code, course_name, credit_hours)
SELECT 'B-CS101', 'B测试计算机基础', 3
WHERE NOT EXISTS (SELECT 1 FROM course WHERE course_code = 'B-CS101');

-- 插入B自己的教学班
INSERT INTO course_class (course_id, teacher_id, term_id, capacity)
SELECT 
    (SELECT course_id FROM course WHERE course_code = 'B-CS101'),
    1001,  -- 假设的教师ID
    'B-2024-2025-1',
    30
WHERE NOT EXISTS (SELECT 1 FROM course_class WHERE term_id = 'B-2024-2025-1' LIMIT 1);

-- 获取B创建的班级ID
SET @b_class_id = (SELECT class_id FROM course_class WHERE term_id = 'B-2024-2025-1' LIMIT 1);

-- 插入选课记录（假设学生ID 2001-2003在sys_user中）
INSERT INTO enrollment (student_id, class_id, term_id, status, grade, grade_status) VALUES
(2001, @b_class_id, 'B-2024-2025-1', 'ENROLLED', 85.5, 'PUBLISHED'),
(2001, @b_class_id, 'B-2024-2025-1', 'ENROLLED', 92.0, 'DRAFT'),
(2002, @b_class_id, 'B-2024-2025-1', 'DROPPED', NULL, NULL),
(2003, @b_class_id, 'B-2024-2025-1', 'ENROLLED', 78.5, 'PUBLISHED');

-- 插入时间槽
INSERT INTO class_time_slot (class_id, week_day, start_slot, end_slot) VALUES
(@b_class_id, 1, 1, 2),  -- 周一1-2节
(@b_class_id, 3, 3, 4);  -- 周三3-4节

SELECT 'B测试数据插入完成' as result;
EOF

# 2. 执行插入命令
mysql -u root -p你的密码 < B_insert_test_data.sql

# 3. 验证插入成功
mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'enrollment表' as table, COUNT(*) as count FROM enrollment UNION SELECT 'class_time_slot表', COUNT(*) FROM class_time_slot;"
```

### **B的每日备份脚本**
```bash
# 每天开始工作前备份
#!/bin/bash
BACKUP_DIR="/path/to/backup"
DATE=$(date +%Y%m%d_%H%M%S)

# 备份B负责的表
mysqldump -u root -p你的密码 ry-vue enrollment class_time_slot > ${BACKUP_DIR}/ry-vue_B_${DATE}.sql
echo "B的备份完成: ry-vue_B_${DATE}.sql"

# 执行命令
chmod +x B_backup.sh
./B_backup.sh
```

### **B验证数据命令**
```bash
# 验证B的所有数据
mysql -u root -p你的密码 << 'EOF'
USE ry-vue;

-- 快速验证
SELECT '=== B的数据验证 ===' as title;
SELECT 'enrollment表' as table_name, COUNT(*) as row_count FROM enrollment
UNION ALL
SELECT 'class_time_slot表', COUNT(*) FROM class_time_slot;

-- 查看样本数据
SELECT '样本数据-enrollment:' as sample;
SELECT enrollment_id, student_id, status, grade_status FROM enrollment LIMIT 3;
EOF
```

---

## 🔄 **数据重置命令（开发过程中）**

### **A重置数据**
```bash
# 当A的测试数据乱了时执行
mysql -u root -p你的密码 < A_insert_test_data.sql
echo "A的数据已重置"
```

### **B重置数据**
```bash
# 当B的测试数据乱了时执行  
mysql -u root -p你的密码 < B_insert_test_data.sql
echo "B的数据已重置"
```

---

## 📋 **每日工作流程**

### **A的每日流程**
```bash
# 1. 早上备份
./A_backup.sh

# 2. 验证数据状态
mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'A数据状态', COUNT(*) FROM term;"

# 3. 开始开发
# ...

# 4. 晚上可选择性备份
mysqldump -u root -p你的密码 ry-vue term course course_class credit_rule > /tmp/A_end_day.sql
```

### **B的每日流程**
```bash
# 1. 早上备份
./B_backup.sh

# 2. 验证数据状态
mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'B数据状态', COUNT(*) FROM enrollment;"

# 3. 开始开发
# ...

# 4. 晚上可选择性备份
mysqldump -u root -p你的密码 ry-vue enrollment class_time_slot > /tmp/B_end_day.sql
```

---

## ✅ **验证成功标准**

### **A验证成功**
```bash
# 执行此命令，应看到类似输出：
# +------------+-----------+
# | table_name | row_count |
# +------------+-----------+
# | term表     | 2         |
# | course表   | 3         |
# | course_class表 | 3    |
# | credit_rule表 | 1     |
# +------------+-----------+

mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'term表' as table_name, COUNT(*) as row_count FROM term UNION SELECT 'course表', COUNT(*) FROM course UNION SELECT 'course_class表', COUNT(*) FROM course_class UNION SELECT 'credit_rule表', COUNT(*) FROM credit_rule;"
```

### **B验证成功**
```bash
# 执行此命令，应看到类似输出：
# +-------------------+-----------+
# | table_name        | row_count |
# +-------------------+-----------+
# | enrollment表      | 4         |
# | class_time_slot表 | 2         |
# +-------------------+-----------+

mysql -u root -p你的密码 -e "USE ry-vue; SELECT 'enrollment表' as table_name, COUNT(*) as row_count FROM enrollment UNION SELECT 'class_time_slot表', COUNT(*) FROM class_time_slot;"
```

---

## ⚠️ **关键要点**

1. **插入时机**：创建完表后立即插入测试数据
2. **备份时机**：每天开始工作前备份自己负责的表
3. **验证方法**：执行验证命令查看数据行数
4. **重置方法**：重新执行插入脚本
5. **独立性**：A和B各自创建完整的数据链，不依赖对方

**注意**：A和B在同一个ry-vue数据库中，但各自操作不同的表，数据互相独立。